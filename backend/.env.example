cat > .env.example << 'EOF'
# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/business_suite

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET_KEY=t > .env.example << 'EOF'
# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/business_suite

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET_KEY=sua-chave-super-secreta-mude-isso-em-producao
JWT_ACCESS_TOKEN_EXPIRES=3600

# Flask
FLASK_APP=app
FLASK_ENV=development
SECRET_KEY=t > .env.example << 'EOF'
# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/business_suite

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET_KEY=sua-chave-super-secreta-mude-isso-em-producao
JWT_ACCESS_TOKEN_EXPIRES=3600

# Flask
FLASK_APP=app
FLASK_ENV=development
SECRET_KEY=outra-chave-secreta-para-flask

# Email (configurar depois)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
EOF

# Email (configurar depois)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
EOF
JWT_ACCESS_TOKEN_EXPIRES=3600

# Flask
FLASK_APP=app
FLASK_ENV=development
SECRET_KEY=outra-chave-secreta-para-flask

# Email (configurar depois)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=

EOF